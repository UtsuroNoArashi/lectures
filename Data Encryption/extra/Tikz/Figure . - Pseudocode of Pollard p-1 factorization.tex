\documentclass{subfiles}
\begin{document}
    \begin{figure}[!hb]
        \centering
        \begin{subfigure}{0.5\textwidth}
            \begin{lstlisting}
                1. Select a smoothness bound --|\(B\)|--.
                2. Randomly pick --|\(a \in \set{2, \ldots, n - 1}\)|-- and compute --|\(d = \gcd{a, n}\)|--.
                    If --|\(d \ge 2\)|-- return it.
                3. For each prime --|\(q \le B\)|--:
                    3.1 Compute --|\(l = \floor*{\tfrac{\log n}{\log q}}\)|--.
                    3.2 Compute --|\(a = a^{q^{l}} \mod{n}\)|--.
                4. Compute --|\(d = \gcd{a - 1, n}\)|--.
                5. For --|\(d = 1, n\)|-- error, else return --|\(d\)|--.
            \end{lstlisting}
        \end{subfigure}
        \caption{Pseudocode for Pollard's \(p - 1\) factorization.}
    \end{figure}
\end{document}
