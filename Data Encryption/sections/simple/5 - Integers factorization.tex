\documentclass{subfiles}
\begin{document}
    In this section we describe some interesting algorithms to factor integers;
        additionally, we describe how Euler algorithm to compute the MCD can be improved
        and how to efficiently compute the modulo of a large integer.

        \subsection{Pollard's \(p - 1\) algorithm}
        \subfile{../sub/5.1 - Pollard algorithm}

        \subsection{Pollard's \(\rho\) algorithm}
        \subfile{../sub/5.2 - Pollard rho}

        \subsection{Lenstra factorization}
        \subfile{../sub/5.3 - Lenstra factorization}

        \subsection{Extended Euclidean algorithm}
        \subfile{../sub/5.4 - Extended Euclidean algorithm}

        \subsection{The Square and multiply algorithm}
        \subfile{../sub/5.5 - Square and multiply}
        \clearpage
\end{document}
