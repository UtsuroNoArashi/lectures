\documentclass{subfiles}
\begin{document}
    The algorithm discussed in this section are called \emph{Pollard's factorization algorithms},
        which is a set of fuzzy algorithms for integer factorization; that is,
        these algorithm may not always work, but if they do, they factorization is very fast.

    \subsubsection{Pollard's $p - 1$ factorization}
    \subfile{../subsub/3.2.1 - Pollard p-1 factorization}

    \subsubsection{Pollard's $\rho$ factorization}
    \subfile{../subsub/3.2.2 - Pollard rho factorization}
\end{document}
