\documentclass{subfiles}
\begin{document}
    Assume we are interested into computing the GCD \(d\) of two integers \(a \text{ and } b\).
        From what we have discussed so far, should be obvious by now,
        that we are also interested into knowing what are the coefficient
        \(\alpha, \beta\) such that
        \[
            d = \alpha a + \beta b.
        \]

    Hence, the classic Euclidean algorithm cannot be used. Thankfully, 
        there exists an \emph{extended} algorithm that solves the problem. 
        We present below the pseudocode.
        \subfile{../../extra/TikZ/Figure . - Pseudo code EEA}
\end{document}
